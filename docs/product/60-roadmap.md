# 60 · 로드맵과 분담

> 팀은 **나 + 개발자 2~3명**. 이 문서는 "누가 무엇을 동시에 할 수 있는가"를 정하는 것이 목적이다.
> 마지막 대조: 2026-08-19

## 지금 상태

| | |
| --- | --- |
| 코드 | `src/` 124파일 · 15,358줄 |
| 테스트 | 33파일 · 257개 · 배포 게이트 |
| 서버 | Supabase 8테이블 · 전 테이블 RLS · Edge Function 1개 |
| 배포 | GitHub Pages (`main` 푸시 → 테스트 → 빌드 → 배포) |
| 기여자 | 1명, 38커밋. **충돌 해결 경험 0** |

## M0 · 팀이 들어올 수 있게 만들기 — **합류 전**

병렬 개발이 시작되기 전에 끝나야 하는 것들. 코드보다 결정과 정리가 대부분이다.

| 항목 | 왜 | 참조 |
| --- | --- | --- |
| **D-1 결정** — 첫 유료 구매자와 공개 Q&A의 관계 | 안 닫으면 페르소나·명세가 두 갈래로 갈라진다 | `00-product-definition.md` |
| **D-2 결정** — 정본 저장소 | remote 2개. `base: '/medi-commu/'`가 한쪽에만 맞다 | 〃 |
| 브랜치 전략과 PR 규칙 문서화 | 충돌 경험이 0인 팀이다 | — |
| 미병합 브랜치 `feat/patient-app-redesign` 처리 | 합류 후에는 더 어려워진다 | — |
| 미커밋 팔레트 변경(네이비→청록, 5파일) 커밋 | 남의 작업과 충돌할 파일이다 | — |
| **F-1 해결** — `bookings.document_types` 타입 갈림 | 새 사람이 DB를 세우면 밟는다 | `40-data-model.md` |

> `src/styles.css`는 3,850줄짜리 단일 파일이다. **여러 명이 동시에 손대면 충돌이 확실하다.** M0에서 분할하거나, 아니면 CSS 담당을 한 명으로 못 박는다. 둘 중 하나는 정해야 한다.

## M1 · 실물을 정직하게 만들기 — 병렬 가능

세 갈래가 서로 다른 파일을 만진다. **동시에 진행할 수 있다.**

### A. 진료 흐름 완성 — `features/doctor` + `state`

| | |
| --- | --- |
| **F-6** 진료 상태 전이 5개 중 1개만 구현됨 | `20-user-flows.md` |
| 의사 화면에 **확인 / 거절 / 진료 마침** 동작 추가 | 지금은 진료가 끝나도 영원히 `in-progress`로 남아 홈에 계속 뜬다 |
| **F-2 · F-3** 결정 — `encounters`의 미사용 컬럼 5개와 `Encounter` 타입을 지울지 살릴지 | 살리면 아래 M2의 "기록 초안"과 이어진다 |
| 검증 | `encounterTrack.test.ts` 확장 + 의사 화면 플로우 테스트 신규 |

### B. 조작감 P0 — `styles.css` + `index.html` + `vite.config.ts`

| | |
| --- | --- |
| I-1 ~ I-10 (10개 항목) | `50-nonfunctional.md` |
| I-1과 I-10은 **같은 커밋** | safe-area를 켜면 sticky 탭이 어긋난다 |
| 검증 | 실기기 설치 PWA에서 하단 내비가 제스처바를 피하는지, 탭 전환에 글자가 덜컹이지 않는지 |

**A와 겹치지 않는다** — A는 `.tsx`, B는 CSS와 설정 파일이다.

### C. 화상 통화를 실제로 쓸 수 있게 — `features/consult` + 인프라

| | |
| --- | --- |
| **TURN 서버 도입** (R-5) | 지금은 대칭 NAT 뒤에서 조용히 실패한다 |
| 시그널링 채널 토큰 (R-3) | 방 이름만 알면 구독된다. 서버가 필요한 일 |
| 화상 진료방 화면 테스트 | 주요 화면 중 유일하게 화면 테스트가 없다 |
| 검증 | **다른 네트워크의 두 사람이 실제로 통화 성공** |

## M2 · 회의록 결론 반영 — M1 이후

`00-product-definition.md`의 "회의록 결론 중 아직 코드에 없는 것" 표를 코드로 옮긴다. **D-1의 답에 따라 우선순위가 갈린다.**

| 항목 | D-1이 (a)면 | D-1이 (b)면 |
| --- | --- | --- |
| 자격 확인 뒤 **사전 문진** (현재 증상 / 이전과 같은 문제인지 / 경고신호 / 요청사항) | P1 | **P0** |
| 의사 일정 카드에 **"진료 전 요약"** (문진 요약 · 확인 필요 항목 · 환자 원문) | P1 | **P0** |
| **의원 운영자 화면** (환자 초대·QR, 문진 현황) | 나중 | **P0 — 화면이 아예 없다** |
| AI 용어를 기능 언어로 교체 | P2 | P2 |

## M3 · 실제 사용자 전 필수 — 파일럿 직전

이걸 다 닫기 전에는 **실제 환자를 태우지 않는다.**

| # | 항목 |
| --- | --- |
| R-2 | 면허 검증을 서버로. 화면에서 스스로 켤 수 있으면 검증이 아니다 |
| R-4 | 브라우저 내장 전사 → 기기 내 모델. 그래야 종단간 암호화가 성립 |
| R-1 | anon key를 env 전용으로 |
| — | `1R 사업계획서` KPI: 의료진 인터뷰 15명, 환자 사용성 테스트 30명, 의원 파일럿 3곳 |

## 병렬 분담 제안

| 사람 | 영역 | 주로 만지는 곳 |
| --- | --- | --- |
| 나 | 제품 명세 소유 · PR 리뷰 · D-1/D-2 결정 진행 · 도메인 로직 | `docs/product/` · `src/domain/` |
| 개발자 1 | 진료 흐름 (M1-A) → 회의록 반영 (M2) | `src/features/doctor/` · `src/state/` |
| 개발자 2 | 조작감 (M1-B) → 접근성 · 바텀시트 | `src/styles.css` · `index.html` · `vite.config.ts` · `src/components/` |
| 개발자 3 | 화상·인프라 (M1-C) → 보안 항목 (M3) | `src/features/consult/` · `supabase/` |

**충돌이 나는 지점** — `src/styles.css`(단일 3,850줄) · `src/App.tsx`(라우트 추가가 몰림) · `src/domain/types.ts`. 이 셋은 한 사람이 소유하거나 변경 전에 알리는 규칙을 둔다.

## 이 문서를 갱신하는 때

- D-1 / D-2가 닫혔을 때 (M2 우선순위가 확정된다)
- 마일스톤이 끝났을 때
- `40-data-model.md`의 F-* 또는 `50-nonfunctional.md`의 R-* / I-*가 해결됐을 때 — **해결한 사람이 그 문서에서 지운다**
