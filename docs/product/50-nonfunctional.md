# 50 · 비기능 요구

> 보안 · 개인정보 · 의료 안전 경계 · 접근성 · **앱 조작감**.
> 마지막 대조: 2026-08-27 · 대상 커밋 `f018a58` + 미커밋 팔레트 변경

## 의료 안전 경계

`00-product-definition.md`의 원칙과 `05-decisions.md`의 제약 중 **코드가 실제로 지키는 것들**이다. **이 경계를 넘는 PR은 반려한다.**

원칙은 우리가 정한 것이라 우리가 바꿀 수 있다. **제약(C-*)은 우리가 정한 것이 아니라 바꿀 수 없다.** 아래를 두 표로 나눠 두는 이유가 그것이다.

### 임상 경계 — 진단하지 않는다

| 경계 | 어디서 지키는가 |
| --- | --- |
| 진단명·질병 확률·중증도 지수를 만들지 않는다 (**C-5**) | `triage`가 진료과 후보까지만 반환. Edge Function은 10개 진료과 enum으로 제한 |
| 응급 판정은 규칙이 한다 | `triageRules`의 `RedFlagRule`. 모델 경로를 타지 않는다 |
| 근거는 환자가 쓴 말만 | 함수에서 한 번, 화면에서 한 번 이중 필터 |
| 답변 문구에 진단명 없음 | `answerTemplates` |
| EMR로 넘기는 것은 환자 말 + 키워드 + 의사 문장 | `emrExport` |
| 통증 척도로 판정하지 않음 | 저장만 하고 어떤 함수도 읽지 않는다 |
| 응급 신호가 걸리면 119 안내를 먼저 | `/ask` 결과 화면 |

**척도는 출판된 설명이 있는 것만 쓴다** (NRS · mMRC · Bristol). PHQ-9 / GAD-7 같은 선별 도구는 넣지 않는다 — 넣는 순간 이 앱이 선별 도구가 된다.

### 법적 제약 — C-1 ~ C-5

근거 법령과 판례는 `05-decisions.md`에 있다. 여기는 **코드의 어디가 그것을 지키는지**만 적는다.

| ID | 제약 | 코드의 어디가 지키는가 |
| --- | --- | --- |
| **C-1** | 진료 성사에 연동된 대가를 받지 않는다 | 결제·정산·수수료 코드가 없다. D-4가 결제 화면 자체를 만들지 않기로 했고, 남아 있던 `/me/payment`는 **삭제 대상(G-11)**이다 — 지울 때까지는 마스킹된 데모 값만 보여 준다 |
| **C-2** | 특정 의료기관·의료인 **선택을 유도하지 않는다** — 대가 여부와 무관 | 매칭은 `doctorFeed`의 **사연 → 의사 인박스** 한 방향뿐이고, `FeedReason`이 왜 걸렸는지 밝힌다. 정렬은 응급 우선 하나 외에 없다. 예약 버튼은 `DoctorProfileScreen`에만 있고 `QuestionDetailScreen`에는 없다 — 답변과 예약이 분리돼 있다 |
| **C-3** | 의사 프로필 필드는 제57조3항 심의 예외 항목으로만 구성한다 | **아직 다 지키지 못한다.** 평점·후기·전후사진·가격·홈페이지 링크는 코드에 없다. 그러나 `Doctor`의 `bio`·`consultStyle`·`career`는 심의 예외 목록 밖이고, 답변 본문의 병원명 필터와 그 로그도 없다 |
| **C-4** | 진료기록의 작성 주체는 의료인이다 | `emrExport`의 `recordDraft`는 의사가 쓴 답변 본문 그대로다. 전사에서 만든 것은 초안이고 의사가 고쳐 쓰는 출발점이다 |
| **C-5** | 진단명·질병 확률·중증도 지수를 출력하지 않는다 | 위 임상 경계 표 전체 |

**C-2를 지키는 것 중 아직 없는 것이 하나 있다** — 하루 답변 수 상한. **숫자는 정해졌다(5회/일, `05-decisions.md` D-8). 코드에 없을 뿐이다.** 잔여 횟수와 초기화 시점을 `/doctor/inbox` 상단에 상시 노출하는 것까지가 요구사항이며, 값은 `src/data/rules/*`에 `asOf`와 함께 둔다.

**C-3은 코드가 따라가야 할 항목이다.** 프로필 필드 정리와 병원명 필터·필터링 로그는 `30-feature-spec.md`·`40-data-model.md` 개정에서 다룬다.

## 개인정보

### 서버에 두는 것 / 안 두는 것

| | 어디에 | 근거 |
| --- | --- | --- |
| 사연 본문, 문진 답, 답변 | Supabase | 여럿이 같이 보는 것이 목적 |
| 사전 확인 답, 주소·지역 | **기기 안에만** (`localStore`) | 사는 지역과 질환 예외는 사람에 관한 값이고, 이 DB는 해외 리전이며 의료정보를 다룰 접근통제가 없다 |
| 결제수단 | 어디에도 안 둔다 | 마스킹된 데모 값만 화면에. **화면 자체가 삭제 대상이다** — G-11 |
| 온보딩 완료 여부 | `localStorage` | 화면 표시용 플래그 |

### 위험 수용 항목 (지금 알고도 두는 것)

| # | 무엇 | 왜 지금은 두는가 | 언제 닫는가 |
| --- | --- | --- | --- |
| R-1 | **anon key가 번들에 하드코딩** (`src/data/supabaseClient.ts`) | RLS가 실제 게이트이고 service_role은 없다 | 실제 사용자 데이터가 들어가기 전 |
| R-2 | **면허 검증을 화면에서 스스로 켤 수 있다** (migration-004) | 모여서 테스트할 때 매번 SQL로 승격시키면 진행이 끊긴다 | **실서비스 전환 시 가장 먼저** |
| R-3 | **시그널링 채널이 방 이름만으로 구독 가능** | 단기 토큰 발급이 필요하고 그건 서버가 필요한 일 | 실제 진료 전 |
| R-4 | **브라우저 내장 음성인식** — 음성이 브라우저 제조사 서버로 간다 | 데모에 자체 모델을 붙일 수 없다 | **실제 환자 대화 전.** 기기 내 모델이어야 종단간 암호화가 성립 |
| R-5 | **TURN 없음** — 대칭 NAT 뒤에서 통화 실패 | 데모 범위 | 파일럿 전 |
| **R-6** | **비공개 덧붙임의 왕복 상한과 표현 필터를 서버가 강제하지 못한다.** 순서와 방향은 `private_threads` RLS가 막지만 **횟수(3왕복)·길이(500/400자)·금지 표현(PT-1~PT-5)은 브라우저에서만 판정한다.** 브라우저를 거치지 않고 API를 직접 부르면 4왕복째가 들어가고 금지 표현이 그대로 저장된다 | **의도한 나눔이다.** 이 값들은 유권해석이 오면 움직인다. `check (round <= 3)`을 걸면 값이 바뀔 때마다 마이그레이션이 필요해지고, 그건 D-6이 「마이그레이션 없이 끄거나 넓힐 수 있어야 한다」고 못 박은 것과 어긋난다. **움직이지 않는 것(누가·순서·방향)은 SQL이, 움직이는 것(횟수·길이·표현)은 규칙 파일이** 맡는다 | **실제 사용자 전.** 서버(Edge Function 또는 트리거)가 규칙 파일을 읽고 같은 판정을 한 번 더 한다 — `60-roadmap.md` M3 |
| **R-7** | **민감정보 자동 파기 장치가 없다.** `private_messages`·`private_threads`는 마지막 발화로부터 6개월, `expression_filter_hits`는 5년으로 정했으나 **기간 경과 파기를 실행하는 것이 없다** | 기간 경과 파기에는 `pg_cron` 같은 스케줄러가 필요하고 이 데모에는 없다. **지금의 통제는 데모에 실제 환자 정보를 넣지 않는 것**이다 | **실제 사용자 전.** 통제가 「안 넣는다」인 이상, 넣는 순간 통제가 사라진다 |

R-2 · R-3 · R-4 · **R-6 · R-7**은 **실제 환자를 태우기 전에 반드시 닫아야 하는 것들**이다. R-1 · R-5는 그 다음이다.

> **하루 답변 상한(D-8 · 5회/일)도 같은 구멍이다.** 값이 `src/data/rules/*`에 있고 판정이 브라우저에서 돈다. 화면(G-6)만 만들면 API를 직접 부르는 것으로 6번째 답변이 들어가고, **그러면 상한을 규제 자산으로 쓰는 논거(C-2 파생 4)가 무너진다.** 서버 강제는 R-6이 한 몫으로 맡는다 — 규칙 파일을 서버가 읽는 모양이어야 값이 바뀔 때 마이그레이션이 필요 없다는 조건도 같다.

> R-6 · R-7은 `40-data-model.md`가 migration-006 초안을 쓰면서 찾아 이 문서로 넘긴 것이다. **둘 다 006이 적용되기 전에는 발생하지 않는다** — 막을 표가 아직 없기 때문이다. 006을 적용하는 순간부터 열린 위험이 된다.

### 동의

화면에 적은 것과 실제로 일어나는 일이 **같아야 한다.** 서버로 보내면서 브라우저에만 둔다고 적는 것은 문구 오류가 아니라 동의를 잘못 받은 것이다. 증상 글이 모델 제공자에게 간다는 사실은 작성 화면에 적는다 — 구현 세부가 아니라 동의 사항이다.

---

## 앱 조작감 — 백로그

**배경.** 설치형 PWA로 써 보면 "앱 같지 않다"는 지적이 나온다. 원인을 추적한 결과 **플랫폼 한계가 아니라 아직 안 쓴 CSS**였다. `src/styles.css`에 `@keyframes`가 **0개**다. 앱 전체에서 스스로 움직이는 것이 하나도 없다.

기반은 이미 깔려 있다 — 모션 토큰(`styles.css:39-40`), tappable 전역 transition(`:94`), `@media (hover: hover)` 게이팅(`:114`), `prefers-reduced-motion` 리셋(`:2815`), 44px 터치 타깃(`:2825`). **표현만 비어 있다.**

**P0 10개(I-1~I-10)는 M1-B에서 닫혔고 이 목록에서 지웠다.** `viewport-fit=cover`로 죽어 있던 safe-area 7규칙을 켜고, 매니페스트 색을 코드 쪽 정본에 맞추고, 터치 기본동작 셋(탭 하이라이트·더블탭 줌·롱프레스 선택)을 누를 수 있는 것에서 끄고, `overscroll-behavior-y`를 `body`에 걸고, 활성 탭의 `font-weight` 전환을 없애고, 미게이팅 hover 3곳을 `@media (hover: hover)`로 묶었다. 남은 것은 아래 13개다.

### P1 — 체감의 대부분

| # | 항목 | 근거 |
| --- | --- | --- |
| I-11 | 진짜 press 상태 (scale + 배경) | 지금은 `:111` `translateY(1px)`뿐. **리플이 아니라 scale로** — 리플은 안드로이드 관용구고 iOS에서는 이질적이다 |
| I-12 | 스켈레톤 + shimmer | `@keyframes` 0개. 로딩은 `App.tsx:52` `불러오는 중…` 텍스트뿐. `aria-busy`도 0회 |
| I-13 | 토스트: `.app-notice`를 밀어내기 → 띄우기 | `styles.css:2463`. 지금은 `max-height: 0 → 120px` 인플로우 확장이라 **페이지가 아래로 밀린다.** 자동 소멸도 없다 |
| I-14 | 라우트 전환 `document.startViewTransition` + reduced-motion 폴백 | `App.tsx:125` 순수 `<Routes>` 교체 |
| I-15 | 라우트 변경 시 스크롤 최상단/복원 | 없다. 상세 화면에 중간부터 들어가는 일이 생긴다 |
| I-16 | 가로 스트립 3곳에 `scroll-snap` + `overscroll-behavior-x: contain` + 스크롤바 숨김 | `:1265` `.record-draft` · `:1666` `.segment-tabs` · `:2590` `.date-strip`. 지금은 안드로이드 뒤로가기 제스처로 새어 나간다 |
| I-17 | `.transcript-list`에 `overscroll-behavior: contain` | `:1196`. 스크롤이 페이지로 체이닝된다 |
| I-18 | `DoctorInboxScreen.tsx:99`의 `<div>` → `<button>` | `cursor: pointer`만 붙고 누름 피드백도 키보드 접근도 없다. 다른 화면에서는 `<button>`이다 |

### P2

| # | 항목 |
| --- | --- |
| I-19 | `navigator.vibrate` 얇은 래퍼 (Android 동작, **iOS는 no-op**) |
| I-20 | 바텀시트 컴포넌트. 현재 `<dialog>` 0개, `role="dialog"` 0개, 포커스 트랩 없음 |
| I-21 | `<details>` 열림 높이 애니메이션 (`:2336`. 지금은 하드 스냅) |
| I-22 | Pretendard 셀프호스팅 + workbox precache. 지금은 `cdn.jsdelivr.net` 외부 로드라 **오프라인 첫 실행에서 시스템 폴백 폰트로 뜬다** (`index.html:11-16`, `vite.config.ts:42`은 로컬 빌드 산출물만 프리캐시) |
| I-23 | `apple-mobile-web-app-*` 메타와 `apple-touch-icon` (iOS 설치 대응) |

### PWA로는 못 하는 것 (네이티브 쉘이 필요한 지점)

- **iOS 햅틱** — Vibration API 미지원
- **iOS 푸시** — 홈화면 설치 + iOS 16.4+ 조건부, 백그라운드 제약
- **앱스토어 배포** — 비대면진료 제품의 신뢰·유통 채널
- **통화 중 백그라운드 유지 / 수신 화면**
- 백그라운드 실행, 정밀 위치

**위 조작감 항목 13개 중 이 목록에 걸리는 것은 I-19 하나뿐이다.** 결정은 `adr/0001-pwa-vs-native-shell.md`로 미룬다.

---

## 접근성

**있는 것** — `:focus-visible` 3px 아웃라인(`:76`) · 44px 최소 터치 타깃(`:2825`) · `prefers-reduced-motion` 전역 리셋(`:2815`) · 네이티브 `<details>` · `accent-color`

**없는 것**

- `aria-busy` 0회 — 로딩 상태를 스크린리더가 모른다
- 포커스 트랩 없음 (모달이 없어서 아직 필요 없지만 I-20과 함께 필요)
- `DoctorInboxScreen.tsx:99`의 클릭 가능한 `<div>` (I-18)
- 라우트 변경 시 포커스 이동/알림 없음

## 성능

- 런타임 의존성 4개뿐: `react` `react-dom` `react-router-dom` `@supabase/supabase-js`. **애니메이션 라이브러리 없음** — 위 백로그는 전부 CSS와 플랫폼 API로 해결한다. framer-motion을 들이지 말 것
- Realtime 핸들러가 **스냅샷 전체를 다시 읽는다** (`liveRepository.ts:259`). 이 규모에서는 의도된 단순함이지만, 사연이 수천 건이 되면 바꿔야 한다
- 분류기 4초 타임아웃 — 분류가 늦다고 사연을 못 올리게 두지 않는다

## 테스트

- 현재 33파일 · 257개. `npm run test:run`이 배포 게이트다 (`.github/workflows/deploy-pages.yml`)
- 테스트는 항상 데모 모드에서 돈다 (`isLiveMode`에 `VITEST` 가드). **네트워크를 타지 않는다**
- `testTimeout: 40000` — `userEvent.type`이 리렌더를 많이 일으켜 문진 테스트가 느리다
- **빈 곳**: 화상 진료방 화면 테스트 없음(도메인만) · 라이브 모드(Supabase) 경로 테스트 없음 · RLS 정책 테스트 없음

## 브라우저·기기

- 대상: Android Chrome 설치형 PWA가 1순위. 데스크톱은 `PreviewToolbar`로 앱 프레임 미리보기 제공
- **iOS는 아직 대상 밖**으로 두고 있다 — 설치 안내도 햅틱도 푸시도 없다. I-23이 최소한의 대응
- 라우팅은 `HashRouter` (GitHub Pages에 서버 리라이트가 없다)
