# 커밋 규칙

혼자 만들어도 **최종 검토는 남에게 맡긴다.** 검토자에게는 맥락이 없다. 아래 네 줄은 충돌을 막으려는 것이 아니라 **읽힐 수 있는 커밋을 만들려는 것**이다.

- 한 커밋이 **한 가지만** 한다. G-2와 G-11을 같은 커밋에 넣는 것은 예외이고, 그 이유(같은 파일·관문)를 메시지에 적는다
- 메시지는 **무엇이 아니라 왜**를 적는다. 무엇은 diff가 말한다
- 화면을 바꾸는 커밋에는 **어느 G-번호인지** 적는다. 검토자가 `docs/product/30-feature-spec.md`에서 목표를 찾을 수 있어야 한다
- **되돌릴 수 없는 것**(`supabase/*.sql` 적용, 컬럼 삭제)은 검토 전에 하지 않는다

근거와 맥락은 [`docs/product/60-roadmap.md`](docs/product/60-roadmap.md) §최종 검토를 남에게 맡긴다에 있다.
